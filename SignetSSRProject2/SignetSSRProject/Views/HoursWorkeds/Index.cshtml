@model IEnumerable<SignetSSRProject.Models.HoursWorked>

@Scripts.Render("~/bundles/hoursWorked")

@{
    ViewBag.Title = "Record Hours Worked";
}

<h2>Record Hours Worked</h2>

<div id="jsGrid">

    <script>


        $(function() {
 

            $("#jsGrid").jsGrid({
                width: "100%",
                height: "400px",

                filtering: true,
                editing: true,
                sorting: true,
                paging: true,
                autoload: true,
                pageSize: 25,
                pageButtonCount: 5,
                deleteConfirm: "Do you really want to delete the record?",

               // controller: employeeInfo,

                controller: {
                    loadData: function (filter) {
                        return $.ajax({
                            type: "GET",
                            url: "/HoursWorkeds/HoursWorkedData",
                            data: filter,
                            dataType: "json"
                        });
                    },

                    insertItem: function (item) { },

                    updateItem: function (item) { },

                    deleteItem: function (item) { },
                },

                fields: [
                    { name: "Date", type: "text", width: 50 },
                    { name: "FirstName", type: "text", width: 75 },
                  //  { name: "LastName", type: "text", wideth: 75 },
                    { name: "HoursWorkedRT", type: "text", width: 50 },
                    { name: "HoursWorkedOT", type: "text", width: 50 },
                    { name: "Description", type: "text", width: 50 },
                    { name: "JobNumber", type: "text", width: 60 },
                    { type: "control" }
                ]
            });
        });




    </script>
</div>


@*<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HoursWorkedRT)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HoursWorkedOT)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Employee.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ItemNumber.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Job.JobNumber)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HoursWorkedRT)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HoursWorkedOT)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Employee.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ItemNumber.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Job.JobNumber)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HoursWorkedID }) |
            @Html.ActionLink("Details", "Details", new { id=item.HoursWorkedID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HoursWorkedID })
        </td>
    </tr>
}

</table>*@
